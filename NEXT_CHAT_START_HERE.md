# NEXT CHAT START HERE

Current stable point:

```text
SITE-1 — kurgin-streamlit-mvp can read public_stones_v1.csv
Admin Checkpoint 33 — 8A manual publish package completed
Manual pilot with real public rows is pending
```

Current rule:

```text
Do not add Timeweb DB, PDF, assets, orders, reserves, auto-sync or specialist cabinet before V1 public catalog dry-run.
```

Next practical step:

```text
DATA-1 — manual dry-run through kurgin-data:
1. create/download a small public_stones_v1.csv from Admin package;
2. place it in kurgin-data root;
3. place publish_manifest.json in kurgin-data root;
4. commit/push kurgin-data;
5. verify kurgin-streamlit-mvp shows real public cards.
```

Do not replace legacy stones.csv silently.
